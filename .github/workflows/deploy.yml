# name: Deploy to GitHub Pages

# on:
#   # Chạy khi có code được push hoặc merge vào nhánh main
#   push:
#     branches: ["main"]
#   # Cho phép chạy thủ công từ tab Actions
#   workflow_dispatch:

# # Cấp quyền cho GITHUB_TOKEN để deploy
# permissions:
#   contents: read
#   pages: write
#   id-token: write

# # Chỉ cho phép một lần deploy chạy tại một thời điểm
# concurrency:
#   group: "pages"
#   cancel-in-progress: true

# jobs:
#   # Job 1: Build ứng dụng
#   build:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: 'npm'

#       - name: Install dependencies
#         run: npm ci

#       - name: Build project
#         run: npm run build

#       - name: Upload artifact
#         uses: actions/upload-pages-artifact@v3
#         with:
#           path: './dist' # Vite build ra thư mục dist

#   # Job 2: Deploy lên GitHub Pages
#   deploy:
#     environment:
#       name: github-pages
#       url: ${{ steps.deployment.outputs.page_url }}
#     needs: build
#     runs-on: ubuntu-latest
#     steps:
#       - name: Deploy to GitHub Pages
#         id: deployment
#         uses: actions/deploy-pages@v4